[project]
name = "audio-processor"
version = "0.1.0"
description = ""
authors = [
    {name = "dhrutiweye",email = "dhrutiman.sethi@wheelseye.com"}
]
readme = "README.md"
requires-python = "==3.10.12"
dependencies = [
    "librosa (>=0.11.0,<0.12.0)",
    "helper @ ../../libs/helper"
]

[tool.poetry]
packages = [{include = "audio_processor", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
